include("mocked_functions.jl")
# functions that are used to communicate with programs outside julia repl

# After writing tests I need to set up github container to locate space and distributedworkflow? docker hub spack setup on github. Runner image for spack. before script for the runner image
